class Snake {
    constructor(size){
        this.size = size;
        this.tail = [];
    }

    draw() {
        ctx.clearRect(0,0,ctxWidth, ctxHeight);
        this.tail.forEach(block => {
            
        })
    }
}